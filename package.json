{
  "name": "ethapp",
  "private": true,
  "scripts": {
    "contracts:compile": "lerna run compile --scope=@ethapp/contracts",
    "contracts:migrate": "lerna run migrate --scope=@ethapp/contracts",
    "contracts:test": "lerna run test --scope=@ethapp/contracts",
    "frontend:start": "lerna run start --scope=@ethapp/frontend",
    "frontend:typecheck": "lerna run typecheck --scope=@ethapp/frontend"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": ["packages/*"]
}
